import { Request, Response } from "express"
import { advGeneration } from "../services/advGeneratorService.js";

const advGenerator = async (req: Request, res:Response) => {
const service = advGenerator
}

export default advGenerator;